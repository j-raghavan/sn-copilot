# Copilot key file — DeepSeek (text-only).
#
# Copy this file to /storage/emulated/0/MyStyle/SnCopilot/ on the
# Supernote (USB or cloud sync), replace KEY with your real DeepSeek
# API key, optionally adjust MODEL, then tap the Copilot sidebar
# button. Nothing else to do.
#
# - DeepSeek has no vision endpoint, so only the page text (typed
#   text + handwriting OCR) is sent — never the page image.
# - As a small mitigation on this text-only path, emails and 7+ digit
#   runs in the outbound text are scrubbed automatically.
# - Lines starting with '#' are comments. Blank lines are ignored.

provider=deepseek
model=deepseek-chat
key=sk-REPLACE-ME
